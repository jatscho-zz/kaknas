# from kaknas.app import app

# app.add_url_rule('/', 'index', index)
# app.add_url_rule('/github', 'github', github)
# app.add_url_rule('/health', 'health', health)

# Look into blueprints later on so we can reference the app instance.
# For now, routes are defined in create_app()
